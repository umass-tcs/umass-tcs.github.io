---
title: Courses
layout: base.njk
---
{% import "macros.njk" as m %}

<section class="hero">
  <h1 class="long-form">Courses</h1>
  <p>Theory courses at UMass we recommend, and what you need before taking them.</p>
</section>

<section class="section">
  {{ m.eyebrow("Courses at UMass") }}
  <ul class="row-list">
    {% for course in courses %}
    {{ m.courseRow(course) }}
    {% endfor %}
  </ul>
</section>
